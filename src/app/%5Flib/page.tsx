/*

Note - if you want to include an underscore in the URL, you can prefix folder name with %5F , which is the URL-encoded form of an underscore.
And acessible on the url with _lib

*/ 
export default function UnderscoreRoute() {
    return (
        <h1>Public Route underscore used.</h1>
    );
}